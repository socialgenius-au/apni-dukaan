[build]
builder = "NIXPACKS"
buildCommand = "npm run build"

[deploy]
startCommand = "npx serve dist -p $PORT --single --rewrite '/merchant=merchant.html' --rewrite '/dashboard=merchant.html' --rewrite '/settings=merchant.html'"
healthcheckPath = "/"
